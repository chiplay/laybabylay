# Fly.io config — PRODUCTION app for laybabylay (www.laybabylay.com).
# Deploy:  fly deploy -c fly.prod.toml
app = "laybabylay-prod"
primary_region = "iad"

[build]

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "suspend"
  auto_start_machines = true
  min_machines_running = 1
  processes = ["app"]

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 512
